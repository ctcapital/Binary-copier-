.venv/
__pycache__/
*.pyc

# Contains your Deriv API token and Telegram api_hash — never commit.
data/
session/
*.session
*.session-journal

# Streamlit secrets — never commit credentials
.streamlit/secrets.toml
secrets.toml
